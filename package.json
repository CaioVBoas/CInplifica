{
  "name": "cinplifica",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "setup": "./scripts/setup-dev.sh",
    "api": "npm run dev --workspace=api",
    "web": "npm run dev --workspace=web"
  }
}
